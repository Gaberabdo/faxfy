enum UploadPlace {
  postImages(1),
  petsImages(2),
  uploads(3),
  usersImages(4),
  postVideos(5),
  commentImages(6),
  clinicImage(7),
  messageImage(8),
  messageRecord(9);

  final int value;

  const UploadPlace(this.value);
}
